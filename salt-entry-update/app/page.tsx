import SaltHeroV2 from '@/components/SaltHeroV2';

/* Client component: o hero v2 (fundo WebGL + headline com sweep prateado)
   substitui a versão anterior (Atmosphere + Decision). Mantido como o
   único conteúdo da rota — Next.js/Vercel continuam intactos. */
export default function Page() {
  return <SaltHeroV2 />;
}
