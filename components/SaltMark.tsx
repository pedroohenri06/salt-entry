/* eslint-disable @next/next/no-img-element */
/* Wordmark oficial. Entra por máscara — nunca por fade. */
export default function SaltMark() {
  return (
    <div className="mark">
      <img src="/brand/salt-word.png" alt="Salt" width={881} height={450} fetchPriority="high" />
    </div>
  );
}
